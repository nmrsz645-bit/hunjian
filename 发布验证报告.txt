自动剪辑发布验证报告

版本：20260712_final_review_fix
发布目录：程序根目录（便携式相对路径，不绑定开发机盘符）

本次最终审查修复：
1. 阿里云任务端点仅允许官方HTTPS转写地址；设置保存、连接测试和运行时在发送Authorization前统一校验。
2. 阿里云句子及词级时间戳缺失、非数值、倒序或短于50毫秒时立即失败，不再修复为0至0.05秒字幕。
3. FastSinglePass=false时预先计算有效输出路径，并将成品写入不会随工作目录清理而删除的输出目录。
4. 最终字幕门禁直接校验FFmpeg实际读取的磁盘SRT事件；每个源事件必须单行并符合当前像素安全宽度。
5. 字幕缓存身份包含规范化音频完整路径和音频内容SHA-256，最终缓存键使用固定长度哈希。
6. HTTP 5xx及TaskCanceledException按临时错误处理，每个网络阶段最多尝试3次，等待2秒、5秒。
7. 上传凭证、上传、任务提交、轮询和结果下载分阶段重试；获得task ID后轮询重试不再重新上传或提交。
8. aliyun_only完全忽略同名TXT，只使用阿里云文字和时间轴；仅text_preferred读取TXT作为优先文字。
9. EnableSubtitles=false继续作为合法用户选项，可生成无字幕视频。

发布验收命令：
- powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\Test-All.ps1
- 使用PowerShell Parser对程序根目录全部.ps1及.psm1文件执行AST解析

自动化验证结果（2026-07-12）：
- Test-All共8个测试脚本全部通过，输出ALL TESTS PASSED。
- 程序根目录9个PowerShell文件AST解析全部通过。

剩余人工验收：
- 已使用真实阿里云API Key和56.35秒中文音频完成端到端识别、字幕烧录与横竖屏抽帧中的竖屏验收。
- 同一次阿里云识别生成6条1080x1920成品，6/6检查通过；抽帧确认字幕为正常简体中文、无乱码、单行显示。
- 对外版不包含API Key、测试音视频、测试成品、字幕缓存或运行日志；复制到新电脑后需要由当前Windows用户重新填写API Key。
